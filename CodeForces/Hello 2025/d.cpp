#include <iostream>
#include <vector>

using namespace std;

int main() {
  int t;
  while (t--) {
	int n, q;
	cin >> n >> q;
	vector sizes{n, 0};
	for (int i = 0; i < n; i++)
	  cin >> sizes[i];

	for (int i = 0; i < q; i++) {
	  int p, x;
	  cin >> p >> x;
	}
  }
}