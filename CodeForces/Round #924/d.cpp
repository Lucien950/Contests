#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
	int string_size, query_count;
	string s;
	cin >> string_size >> query_count >> s;
	for (int query_i = 0; query_i < query_count; query_i++) {
	  int l, r;
	  cin >> l >> r;
	}
  }
}